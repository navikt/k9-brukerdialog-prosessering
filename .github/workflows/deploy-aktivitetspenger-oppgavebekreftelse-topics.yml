name: Deploy aktivitetspenger oppgavebekreftelse topics

env:
  TOPICS_PATH: 'nais/topics/aktivitetspenger-oppgavebekreftelse-topics.yml'

on:
  push:
    paths:
      - '.github/workflows/deploy-aktivitetspenger-oppgavebekreftelse-topics.yml'
      - '.github/workflows/reusable-topic-workflow.yml'
      - 'nais/topics/aktivitetspenger-oppgavebekreftelse-topics.yml'

jobs:
  deploy-aktivitetspenger-oppgavebekreftelse-topics:
    permissions:
      id-token: write
    uses: ./.github/workflows/reusable-topic-workflow.yml
    with:
      job-name: aktivitetspenger-oppgavebekreftelse-topics
      topics-path: 'nais/topics/aktivitetspenger-oppgavebekreftelse-topics.yml'
    secrets: inherit
