name: Deploy aktivitetspengersøknad topics

env:
  TOPICS_PATH: 'nais/topics/aktivitetspenger-soknad-topics.yml'

on:
  push:
    paths:
      - '.github/workflows/deploy-aktivitetspenger-soknad-topics.yml'
      - '.github/workflows/reusable-topic-workflow.yml'
      - 'nais/topics/aktivitetspenger-soknad-topics.yml'

jobs:
  deploy-aktivitetspenger-soknad-topics:
    permissions:
      id-token: write
    uses: ./.github/workflows/reusable-topic-workflow.yml
    with:
      job-name: aktivitetspenger-soknad-topics
      topics-path: 'nais/topics/aktivitetspenger-soknad-topics.yml'
    secrets: inherit
